<?php

declare(strict_types=1);

/*
 * Pest bootstrap. Tests under tests/Unit run against pure domain
 * code (no framework, no I/O). Tests under tests/Application will
 * land in PR 2 alongside use-cases and in-memory adapters.
 */
